// Test-only stub. Real builds use the actual `server-only` package, which
// enforces the server/client boundary at build time; vitest runs in plain
// Node with no such bundler distinction, so this is a harmless no-op.
export {};